stage: encoder
model:
  encoder:
    name: sonata_ptv3
    pretrained: true
    tune_mode: linear_probe
    pretrained_checkpoint: data/pretrained/sonata/sonata.pth
    checkpoint: null
    grid_size: 0.005
    patch_size: 128
    enable_flash: false
output:
  dir: outputs/encoder
